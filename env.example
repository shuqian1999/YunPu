# 云谱 - Docker部署环境变量配置
# 复制此文件为 .env 并修改相应配置

# 后端Secret Key (必填，用于JWT token加密)
SECRET_KEY=your-super-secret-key-change-this-in-production

# 数据库URL (可选，默认使用SQLite)
# DATABASE_URL=sqlite:///./data/yunpu.db

# CORS允许的域名 (多个用逗号分隔)
# CORS_ORIGINS=http://localhost:3000,http://localhost:5173
